import { Title } from "@solidjs/meta";

export default function NewUser() {
    return (
        <main>
            <Title>
                New User
            </Title>
            <h1>
                New User<br/>
                Pretty sure this is not needed if we are auto-assigning their usernames
            </h1>
        </main>
    );
}